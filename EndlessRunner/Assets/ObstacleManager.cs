using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ObstacleManager : MonoBehaviour
{
    //public Transform player;

    void Update()
    {
        //if (player.position.z - transform.position.z > 60f)
        //{
        //    Destroy(gameObject);
        //}
    }
}
