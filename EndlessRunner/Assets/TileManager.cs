using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class TileManager : MonoBehaviour
{
    //public Transform player;

    void Update()
    {
        //// Calculate the direction from the player to the tile
        //Vector3 toTile = transform.position - player.position;

        //// Calculate the forward direction of the player
        //Vector3 playerForward = player.forward;

        //// Check if the tile is behind the player based on the dot product
        //float dotProduct = Vector3.Dot(toTile, playerForward);
        //Debug.Log(player.position.z);
        //// If dot product is negative, the tile is behind the player
        //if (dotProduct < 0 && player.position.z > transform.position.z + 60f)
        //{
        //    Debug.Log("destroyed tile");
        //    Destroy(gameObject);
        //}
    }
}
